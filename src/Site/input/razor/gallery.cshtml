@using Statiq.Razor
@using Site.Models
@inherits StatiqRazorPage<GalleryPage>

<section class="home-gallery-grid photos-page-grid">
    @foreach (var item in Model.Images)
    {
        <a class="image-link" href="@item.Href">
            <img class="image" src="@item.Src" />
        </a>
    }
</section>

<nav class="pager-bar">
    @if (Model.PrevHref != null)
    {
        <a class="text-link" href="@Model.PrevHref">Page @(Model.Index - 1)</a>
    }
    <span class="pager-label">Page @Model.Index</span>
    @if (Model.NextHref != null)
    {
        <a class="text-link" href="@Model.NextHref">Page @(Model.Index + 1)</a>
    }
</nav>